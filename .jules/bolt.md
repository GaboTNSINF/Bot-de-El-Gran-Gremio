## 2024-06-07 - Missing indexes on SQLite foreign keys
**Learning:** SQLite does not automatically index columns used as foreign keys. In `database.py`, `obtener_ladder_dms()` and `obtener_perfil_dm()` performed large `LEFT JOIN`s on the `reseñas_dms` table using the `dm_id` foreign key. Without an index on `dm_id`, this caused a full table scan (`SCAN r LEFT-JOIN`) on the `reseñas_dms` table for every DM, degrading performance significantly as the number of reviews grew.
**Action:** Always verify if queries involving `JOIN`s on foreign keys use indexed columns by running `EXPLAIN QUERY PLAN` on complex analytical queries. Create an index (`CREATE INDEX idx_name ON table(column)`) for foreign keys that are frequently used in `JOIN`s or `WHERE` clauses.
## 2024-06-07 - Missing indexes for ORDER BY clauses
**Learning:** SQLite requires creating an expensive temporary B-tree (`USE TEMP B-TREE FOR ORDER BY`) and performing a full table scan when sorting rows via `ORDER BY` if there is no covering index matching the sort order. In `database.py`, `obtener_ladder_aventureros()` sorts the entire `aventureros` table by `nivel DESC, sesiones_jugadas DESC` to get the top 10 players. As the player base grows, this query becomes a bottleneck.
**Action:** Create composite indexes for columns frequently used together in `ORDER BY` and `LIMIT` clauses to allow the database engine to perform an O(1) index scan (`SCAN ... USING INDEX`) instead of full table file sorts.

## 2024-06-07 - discord.py/pycord get_member_named performance
**Learning:** The `guild.get_member_named(name)` method in `discord.py` / `pycord` is not an $O(1)$ Hash Map lookup. It actually performs a linear scan $O(N)$ across all members in the guild cache. If this method is used in a loop over user inputs (like parsing a list of player names), the complexity becomes $O(N \cdot M)$ where N is the guild size and M is the number of inputs. This causes severe CPU freezing for large guilds if multiple names are not found or typed incorrectly.
**Action:** When needing to look up multiple members by name/nickname from user string inputs, construct a local hash map index (`{m.name.lower(): m for m in guild.members}`) in a single $O(N)$ pass. Then perform all string lookups against this local dictionary in $O(1)$ time per input.

## 2024-06-07 - Premature optimization of combinations
**Learning:** In `cogs/matchmaking.py`, `itertools.combinations(lista, K)` was used to find balanced groups. While this is $O(N^K)$ in theory, attempting to replace it with a sliding window caused a functional regression because the custom grouping logic was highly specific and dependent on encapsulation. The SQL pre-filtering (`MIN(...) - MAX(...) >= 180`) was already reducing the candidate pool drastically, meaning $N$ was always small (e.g., 5-10). The optimization broke functionality for zero real-world performance gain.
**Action:** Do not optimize theoretical algorithmic bottlenecks (like combinations) if upstream database filtering already guarantees a microscopically small dataset. Always trust and maintain encapsulated business logic over theoretical CPU savings.

## 2024-06-07 - Pagination/Ladders with large LEFT JOINs
**Learning:** In `database.py`, `obtener_ladder_dms()` calculated the leaderboard by doing a `LEFT JOIN` on the entire `registro_dms` and `reseñas_dms` tables, grouping the results, and only applying `LIMIT 10` at the very end. This caused SQLite to evaluate $O(N \cdot M)$ join records in memory before discarding 99% of them, which scales poorly.
**Action:** When computing a leaderboard or paginated list that requires heavy joins with child tables, write the SQL query to perform the `ORDER BY ... LIMIT N` on the parent table **in a subquery first**. Then, perform the `LEFT JOIN` and `GROUP BY` only on those pre-filtered top N results. This turns an $O(N \cdot M)$ operation into $O(N \log N + K \cdot M)$, where $K$ is the limit size.

## 2024-06-07 - Inefficient cache lookups in Pycord loops
**Learning:** Using `ctx.guild.get_role(id)` inside a list comprehension to filter roles (e.g. `[ctx.guild.get_role(rid) for rid in list if ctx.guild.get_role(rid) in user.roles]`) is highly inefficient. While `get_role` reads from an internal dict cache, doing it repeatedly inside an iterative logic scales poorly and performs unnecessary property lookups.
**Action:** When filtering a user's existing roles against a list of target IDs, convert the target IDs to a mathematical `Set` ($O(1)$ lookup time) and iterate over `user.roles` directly: `target_ids = set(my_list); roles_to_remove = [rol for rol in user.roles if rol.id in target_ids]`. This evaluates the condition directly against the user's existing role objects without querying the guild cache.
